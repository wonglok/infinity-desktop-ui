@AGENTS.md

must use zustand for state management

must suuport parallel generation when it comes to promise and showign appropriate loading indicator.

must use modal to confirm or to prompt for user input

must always enable esc key for modal close keyboard shortcut.
